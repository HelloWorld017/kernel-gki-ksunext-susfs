name: Pack Boot Image

on:
  workflow_call:
    inputs:
      gki_build_id:
        required: true
        type: string

permissions:
  contents: read

jobs:
  pack:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Check out scripts
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Download built kernel
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: gki-${{ inputs.gki_build_id }}-ksun-susfs
          path: inputs/kernel

      - name: Download stock boot image
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: stock-boot
          path: inputs/stock

      - name: Repack boot.img
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/pack_boot_image.py \
            --stock inputs/stock/stock-boot.img \
            --kernel inputs/kernel/Image \
            --output boot.img

      - name: Upload completed boot image
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: packed-boot
          path: boot.img
          if-no-files-found: error
          compression-level: 0
